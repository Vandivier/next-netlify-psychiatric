import { PageWrapper } from "@components/PageWrapper";

const About = () => (
  <PageWrapper
    headTitle="About"
    MainContent={() => (
      <>
        <p>TODO: TWO-COLUMN</p>
      </>
    )}
  />
);

export default About;
